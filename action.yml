name: 'PR Lint'
description: 'Lint PR Titles'
inputs:
    issuekey:
        description: 'Issue Key'
        required: true
    prtitle:
        description: 'PR Title'
        required: false
runs:
    using: 'node12'
    main: './dist/index.js'
